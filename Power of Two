class Solution {
    public boolean isPowerOfTwo(int n) {
        int i=0;
        double twos = 0;
        while(twos <= n){
            twos = Math.pow(2, i);
            if(twos == n){
                return true;
            }
            i++;
        }
        return false;
    }
}
